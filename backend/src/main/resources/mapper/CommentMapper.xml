<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.backend.comment.mapper.CommentMapper">
    <select id="getBoardForComment" resultType="com.study.backend.board.model.Board">
        SELECT id,
               board_type_id AS boardTypeId,
               state
        FROM board
        WHERE id = #{boardId}
    </select>

    <select id="getCommentById" resultType="com.study.backend.comment.model.Comment">
        SELECT co.id,
               co.content,
               DATE_FORMAT(co.created_date, '%Y-%m-%d %H:%i') AS createdDate,
               m.id AS memberId,
               m.member_name AS author
        FROM comment co
            JOIN member m
                ON co.member_id = m.id
        WHERE co.id = #{commentId}
    </select>

    <select id="getCommentsByBoardId" resultType="com.study.backend.comment.model.Comment">
        SELECT co.id,
               co.content,
               DATE_FORMAT(co.created_date, '%Y-%m-%d %H:%i') AS createdDate,
               m.id AS memberId,
               m.member_name AS author
        FROM comment co
            JOIN member m
                ON m.id = co.member_id
        WHERE co.board_id = #{boardId}
        ORDER BY co.id
    </select>

    <insert id="createComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment(content,board_id,member_id)
        VALUES(#{content},#{boardId},#{memberId})
    </insert>

    <delete id="deleteComment">
        DELETE FROM comment
        WHERE id = #{commentId} and member_id = #{memberId}
    </delete>
</mapper>
